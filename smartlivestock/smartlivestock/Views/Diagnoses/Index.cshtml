@model IEnumerable<smartlivestock.Models.Diagnosis>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DiagName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreateDt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsrName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiagName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreateDt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsrName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DiagId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DiagId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DiagId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
